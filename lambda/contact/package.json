{
  "name": "srnext-bff-contact-lambda",
  "version": "1.0.0",
  "description": "Contact resolver for AppSync",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../lambda.zip . && cd ..",
    "test": "jest"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1404.0",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.5.0",
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.3.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.8.3"
  }
}
